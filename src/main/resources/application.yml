spring:
  datasource:
    url: jdbc:mysql://localhost:3307/wxshop?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    # kebab case
    driver-class-name: com.mysql.cj.jdbc.Driver
    # python 常用的格式 叫做 snake case
    # driver_class_name
    # java 常用的格式 叫做 camel case
    # driverClassName
mybatis:
  config-location: classpath:db/mybatis/config.xml

wxshop:
  redis:
    host: 127.0.0.1
    port: 6380